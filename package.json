{
  "name": "hello-eve",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "imports": {
    "#*": "./agent/*"
  },
  "scripts": {
    "dev": "eve dev",
    "build": "pnpm --filter @jinshuju/eve-oidc build && eve build",
    "start": "eve start",
    "test": "pnpm --filter @jinshuju/eve-oidc test",
    "typecheck": "pnpm --filter @jinshuju/eve-oidc build && tsc",
    "prepare": "pnpm --filter @jinshuju/eve-oidc build"
  },
  "dependencies": {
    "@jinshuju/eve-oidc": "workspace:*",
    "@ai-sdk/anthropic": "4.0.5",
    "@workflow/world-postgres": "5.0.0-beta.20",
    "ai": "7.0.11",
    "eve": "0.24.3",
    "just-bash": "3.0.2"
  },
  "devDependencies": {
    "@types/node": "24.13.2",
    "typescript": "6.0.3"
  },
  "overrides": {
    "ai": "7.0.11"
  },
  "engines": {
    "node": "24.x"
  }
}
