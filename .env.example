ANTHROPIC_API_KEY=your_deepseek_api_key

# Postgres for the workflow world (agent/agent.ts) and the dynamic schedule store.
WORKFLOW_POSTGRES_URL=postgres://user:pass@host:5432/dbname

# Discord channel (proactive delivery of scheduled reminders).
DISCORD_PUBLIC_KEY=...
DISCORD_APPLICATION_ID=...
DISCORD_BOT_TOKEN=...

# HTTP Basic verifier for the eve HTTP routes (agent/channels/eve.ts).
ROUTE_AUTH_BASIC_USERNAME=...
ROUTE_AUTH_BASIC_PASSWORD=...

# Langfuse observability (agent/instrumentation.ts).
# US region base URL; use https://cloud.langfuse.com for EU.
LANGFUSE_PUBLIC_KEY=pk-lf-...
LANGFUSE_SECRET_KEY=sk-lf-...
LANGFUSE_BASE_URL=https://us.cloud.langfuse.com
